# Recipe Dredger Configuration
# Copy this file to .env and customize for your setup

# =============================================================================
# CONNECTION SETTINGS
# =============================================================================

# --- Mealie Configuration ---
MEALIE_ENABLED=true
MEALIE_URL=http://your-mealie-instance.local:9000
MEALIE_API_TOKEN=insert_your_mealie_token_here

# --- Tandoor Configuration ---
TANDOOR_ENABLED=false
TANDOOR_URL=http://your-tandoor-instance.local:8080
TANDOOR_API_KEY=insert_your_tandoor_key_here

# =============================================================================
# SCRAPER BEHAVIOR
# =============================================================================

# WARNING: DRY_RUN defaults to true for safety. Set to false to actually import.
DRY_RUN=true

# Logging verbosity (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Stop after importing this many recipes per site
TARGET_RECIPES_PER_SITE=50

# How many URLs to scan per site before giving up
SCAN_DEPTH=1000

# Filter recipes by language (ISO 639-1 code: en, es, fr, de, etc.)
# Leave empty or comment out to import all languages
# LANGUAGE_FILTER=en

# =============================================================================
# PERFORMANCE & RATE LIMITING
# =============================================================================

# Seconds to wait between requests to the same domain
CRAWL_DELAY=2.0

# Honor robots.txt crawl-delay directives
RESPECT_ROBOTS_TXT=true

# Days before sitemap cache expires (saves time on repeated runs)
CACHE_EXPIRY_DAYS=7

# =============================================================================
# OPTIONAL: CUSTOM SITE LIST
# =============================================================================

# Uncomment to use a custom site list instead of sites.json
# Path relative to container: /app/sites.json (mounted via volume)
# SITES=/app/sites.json

# OR: Comma-separated list (not recommended for large lists)
# SITES=https://example.com,https://another-blog.com

# =============================================================================
# NOTIFICATIONS (Optional)
# =============================================================================

# Webhook URL for run completion notifications (Discord, Slack, ntfy.sh, etc.)
# NOTIFICATION_WEBHOOK_URL=https://discord.com/api/webhooks/insert_your_webhook_url_here

# =============================================================================
# ADVANCED
# =============================================================================

# Sync your existing Mealie/Tandoor library at startup to avoid duplicate API calls
SYNC_LIBRARY=true

# =============================================================================
# VEGAN FILTER + MACRO BANDING (fork additions)
# =============================================================================
VEGAN_ONLY=true          # reject recipes containing animal ingredients
TAG_RECIPES=true         # apply vegan + macro band tags
WRITE_NUTRITION=true     # fill Mealie's nutrition panel (kcal/protein/fat/carb/fibre)

# Which macros get band tags. Options: protein,carb,fat,calorie,fibre
BAND_TAGS=protein,carb,fibre

# Fraction of ingredient lines that must be recognised before an estimate
# is trusted. Below this the recipe is tagged macros-unknown instead.
MIN_COVERAGE=0.7

# Band thresholds, per serving
PROTEIN_HIGH=20
PROTEIN_MED=10
CARB_HIGH=50
CARB_MED=20
FAT_HIGH=25
FAT_MED=10
FIBRE_HIGH=8
FIBRE_MED=4
CAL_HIGH=700
CAL_MED=400
